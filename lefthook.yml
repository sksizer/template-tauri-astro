commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}

pre-commit:
  parallel: true
  commands:
    eslint:
      root: src-astro/
      glob: '*.{js,ts,astro,mjs}'
      run: pnpm eslint {staged_files}
    clippy:
      root: src-tauri/
      glob: '*.rs'
      run: cargo clippy -- -D warnings
    rustfmt:
      root: src-tauri/
      glob: '*.rs'
      run: cargo fmt -- --check
    oxlint:
      root: src-astro/
      glob: '*.{js,ts,astro,mjs}'
      run: pnpm oxlint {staged_files}
    prettier:
      root: src-astro/
      glob: '*.{js,ts,astro,css,json,md,yml,yaml,mjs}'
      run: pnpm prettier --check {staged_files}
    typecheck:
      root: src-astro/
      run: pnpm typecheck
